nm-shared
cockpit --> able of video record

PAM(priviliage access manager) it does AAA and can do 2FA(2 factor authentication)
user --> PAM --> Private-Network

less /etc/protocols (routing protocols)

DAC, MAC

systemcall, vsyscall, vdso, user space, kernel space

strace(track syscalls), lttrace, ptrace
learn the systemcalls shown by strace ls command for example

glibc

dirty call

cat, bat

forensic:
step-1: teriage(information gathering) e.g. --> netstat -antp > tcp.txt, mount > mount.txt
mitigation 

setfacl -d ?

auditd on a command to alert